import Page from 'components/Page';

const Index = (): JSX.Element => (
  <Page protectedResources>
    <div style={{ backgroundColor: 'var(--primary-dark)' }}>Homepage</div>
  </Page>
);

export default Index;
